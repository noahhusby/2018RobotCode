package org.usfirst.frc.team1701.robot.subsystems;

public class ArmClamp {
    /*


    //Make the control a switch to allow the clamp to stay active
    //Set as button on operator side
    //
    //Proximity sensor (see line below)
    //Possibly a light on the board to indicate that there is a crate inside of the clamp
    //




    if(switchClamp == pressed) {
    //Activate clamp
    }else{
    //release clamp
    }

     */

}
